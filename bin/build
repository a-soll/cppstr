#!/bin/bash

install_path="$1"

mkdir -p build
cd build
cmake .. -DCMAKE_BUILD_TYPE=Release
cmake --build .

if [ -z "$install_path" ];
then
    cmake --install .
else
    cmake --install . --prefix=$install_path
fi
